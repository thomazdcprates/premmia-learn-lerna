{
  "name": "premmia-learn-lerna",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "devDependencies": {
    "lerna": "^8.1.0",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "lerna run build",
    "clean": "rm -rf node_modules packages/**/node_modules packages/**/dist",
    "publish-package": "lerna publish --contents dist"
  }
}
